# CLAUDE DIRECTIVE — GDA Estimate Wizard Postal Master R5 CR6-R3F Synthetic Exit Classification Correction

## 1. Status, authority, and supersession

```yaml
phase: GDA_ESTIMATE_WIZARD_POSTAL_MASTER_R5_CR6_R3F_SYNTHETIC_EXIT_CLASSIFICATION_CORRECTION
marker: GDA_ESTIMATE_WIZARD_POSTAL_MASTER_R5_CR6_R3F_SYNTHETIC_EXIT_CLASSIFICATION_CORRECTION_DIRECTIVE_V1
status: GOVERNANCE_ONLY_NOT_AUTHORIZED_FOR_DIAGNOSIS_IMPLEMENTATION_OR_EXECUTION
date: 2026-09-05
repository: nisikawa-officeAZ/GYEON
pull_request: https://github.com/nisikawa-officeAZ/GYEON/pull/67
branch: agent/gda-estimate-ocr-postal-clean-replacement-r1
fixed_predecessor_head: da3a2a158f214c3e51cac87d678cfd9de4bab5ec
fixed_predecessor_tree: 0a735d45dcd67c0e451f581a2e4ac9d900515f3c
base: main
pull_request_state_before_authoring: OPEN
pull_request_draft_before_authoring: true
hosted_target_project_ref: nqvnjqcxgngqsqkbpdfi
production_target_contact: PROHIBITED
supersedes: GDA_ESTIMATE_WIZARD_POSTAL_MASTER_R5_CR6_R3E_DURABLE_BURN_LEDGER_AND_LOGICAL_SPAWN_CORRECTION_DIRECTIVE_V1
claude_result_and_codex_acceptance: https://github.com/nisikawa-officeAZ/GYEON/pull/67#issuecomment-5554790593
supabase_cli_release: v2.116.0
supabase_cli_release_commit: 997a1e6
```

CR6-R3F fully supersedes CR6-R3E. It retains the accepted aggregate, Git
metadata acquisition, protected-content boundary, both-mode isolated
materialization, exact attempt grammar, official CLI byte mapping, durable
burn ledger, logical-versus-real spawn boundary, evidence, two-file
implementation allowlist, and stop rules. It closes the remaining ambiguity by
mapping target-identity drift and execution-readiness drift to separate
existing frozen-core flags without introducing a new exitInfo field.

Authoring this document does not authorize private-file transmission, Claude
invocation, implementation, executable tests, Git or Supabase CLI execution,
network or hosted access, migration replay, stage/commit/push, further PR
mutation, Ready conversion, merge, deployment, cutover, or retirement.

## 2. Accepted diagnosis

The one-time CR6-R3E tool-disabled diagnosis returned
`CHANGES_REQUIRED_DIRECTIVE`. MacBook Codex independently accepted the single
ambiguity but corrected Claude's proposed remedy:

- CR6-R3E grouped executable/argv/project-ref/workdir/cwd/environment identity,
  burn-record validity, lock ownership, stage order, and remaining deadline
  under one validation sentence, then defined only the exact
  `targetMismatch:true` synthetic result.
- Frozen `apply-once.mjs` recognizes `targetMismatch` and
  `ledgerMismatch`; a new `burnOrLockInvalid` field would be ignored and
  fall through to `non_zero_exit:undefined`.
- Therefore CR6-R3F uses only recognized frozen fields: target identity maps to
  `targetMismatch`, while burn/lock/stage readiness maps to
  `ledgerMismatch`. Deadline remains owned by frozen `runWithTimeout`.

The accepted public adjudication is PR #67 comment `5554790593`. No tool,
edit, test, Git/Supabase command, hosted contact, protected-content read, or
mutation occurred inside Claude's diagnosis.

## 3. Objective and exact two modes

Define the smallest safe two-file offline adapter around the accepted fourteen
CR6 files:

1. `preflight-only` locally materializes, inspects, and deletes one fresh
   isolated 112-file tree. It never acquires an execution lock, writes a burn
   record, constructs a linked command, contacts hosted state, or retains
   hosted evidence.
2. `execute-once` is present but not run during implementation. A later Owner
   approval must quote the accepted adapter commit/tree, fixed project ref,
   unused attempt id, exact command sequence, runtime root, evidence root, and
   retained-evidence destination.

Ambiguity, unsupported CLI, malformed or excessive output, prompt, mismatch,
timeout, signal, non-zero exit, redaction failure, hash failure, filesystem
uncertainty, or cleanup uncertainty fails closed. A durably burned attempt is
never unburned or reused. Any real-attempt failure is
`QUARANTINE_NO_RETRY`.

## 4. Immutable repository and migration inputs

The fourteen existing files under
`scripts/e2e/gda-estimate-postal-master-r5-cr6/` remain unchanged read-only
dependencies. Their accepted SHA-256 of byte-sorted `sha256sum` lines is:

`4e4dac8a8d6c667c586fee010ef7a9909c665333547c74f0ac7b37ccf44f2395`

Fixed migration identity:

- formal top-level SQL count: `113`;
- staged count: `112`;
- sole exclusion:
  `supabase/migrations/20260801110110_line_link_tokens.sql`;
- staged aggregate SHA-256:
  `0d5414ac1257a287938e141d5c398f3607c3bf2650d38255f520956f15ddb5bb`;
- protected monthly content SHA-256 attestation:
  `1f0f0f491e0e083c8163cb309b3846c035629c6930e83b180dc2e9ffdab86255`.

Protected metadata-only identities:

| Path | Mode | Blob |
| --- | --- | --- |
| `src/components/estimates/wizard/screens/ScreensPreview.tsx` | `100644` | `c1eb0dc88954f3a17cc85e313b62d5bb6a4fda3f` |
| `supabase/migrations/20260801110110_line_link_tokens.sql` | `100644` | `accd22345054cc44f89156fd78eaba6dfe4242a4` |
| `supabase/migrations/20260807135006_monthly_invoice_pdf_artifact.sql` | `100644` | `32fda49583ae1217bc13711784ad8fa31744726c` |
| `src/lib/monthly-statements/monthly-invoice-artifact-boundary.test.ts` | `100644` | `fe3c80f22fd80dcbfab076082473216dda582c14` |

Never open, read, diff, display, prompt-copy, stage, or modify protected
contents. In either separately approved mode, the fixed monthly Git blob may
be streamed directly into its isolated file without displaying, logging,
retaining, transmitting, or re-hashing its SQL text. The protected LINE blob
is never requested, materialized, or applied.

The monthly digest attestation remains pinned to
`docs/master_specification/ENVIRONMENT_REMEDIATION_PLAN.md` blob
`5f7a39b287a7484b5d2fa8490bf8780bbf5a8f8f`, section 13.2, authority
https://github.com/nisikawa-officeAZ/GYEON/pull/2#issuecomment-5261032333,
HEAD `96c0d5cb34f60396242ea89ae0cf4d0aac92f59e`, tree
`aa544700b66971473f5c7127289bfffd76b8b024`.

## 5. Exact manifest and materialization contract

Build exactly 112 UTF-8 rows in repository-relative path byte order:

```text
<64 lowercase hex content SHA-256><two ASCII spaces><path><LF>
```

There is no header, footer, blank line, quoting, JSON, BOM, or CR. The LINE
path contributes no row. The monthly row uses only its attested digest. The
111 ordinary rows hash exact Git blob bytes. SHA-256 of the concatenated rows
must equal the Section 4 aggregate.

Use `/usr/bin/git`, argument arrays, `shell:false`, closed stdin except exact
`cat-file --batch`, a fixed sanitized environment, fixed cwd, time/byte
ceilings, and only the retained accepted argv. Validate fixed HEAD/tree, branch,
clean status, upstream `0 0`, one accepted governance parent, exact two-file
implementation diff, and PR identity.

The non-recursive migration `ls-tree -z` response must contain exactly 113
top-level SQL blobs plus the fixed `DRAFT_DO_NOT_APPLY` tree
`b6b9b1bd0cefedd0a08a40ef7c2c55c4fa5f4018`. A separate response contains
exactly the two external protected metadata paths. Validate all counts, names,
modes, types, object ids, order, and four protected identities before batch
access.

Use one exact 111-ordinary-object batch for canonical hashes and one exact
112-object materialization batch per separately approved mode, including the
monthly blob and excluding LINE. Validate batch framing and independently
inspect exactly 112 regular mode-100644 isolated files. Canonical and actual
evidence are independent values and must match before any execution plan.

## 6. Public input and internal authority

The public entrypoint accepts exactly:

```text
{ mode, attemptId, confirmation, repoRoot, runtimeRoot, evidenceRoot }
```

- `mode` is `preflight-only` or `execute-once` only;
- preflight `confirmation` is absent/null;
- execute confirmation is exactly `EXECUTE_GDA_POSTAL_R5_CR6_ONCE`;
- `attemptId` matches exactly
  `^[A-Za-z0-9][A-Za-z0-9_-]{0,127}$`;
- roots are absolute, traversal-free, symlink-safe, mutually disjoint, no more
  than 4,096 UTF-8 bytes, and satisfy the frozen owner/mode rules; and
- unknown keys fail closed.

The caller cannot supply or override project ref, fixed HEAD/tree, migration
set, manifests, digests, executable, argv, cwd, environment, Git/process/fs
adapters, parser, timer, clock, ledger root, lock path, evidence sink, or
timeouts/limits. Existing dependency injection is test-only internal behavior.

## 7. Fixed executable and real CLI mapping retained

Use only `/opt/homebrew/Cellar/supabase/2.116.0/bin/supabase`, validated as a
regular non-symlink executable with recorded SHA-256. Local implementation
verification may run only `--version`, `migration list --help`, and
`migration up --help`; require version `2.116.0` and every fixed token.

Real linked execution, when separately authorized, uses `shell:false`, closed
stdin, exact isolated cwd, fixed sanitized environment, bounded collectors, a
shared 1,800,000 ms deadline, and only:

```text
supabase migration list --linked --project-ref <fixed-ref> --workdir <fixed-workdir> --output-format json
supabase migration up --linked --project-ref <fixed-ref> --workdir <fixed-workdir> --yes --output-format json
```

The list result succeeds only with exit zero, no signal/timeout/truncation or
prompt, exactly one UTF-8 JSON object plus LF, exact stderr
`Connecting to remote database...` plus LF, exactly top-level keys
`migrations` and `message`, exact message `Migrations listed`, and exactly 112
canonical local rows with keys `local`, `remote`, `time`, every remote empty,
and exact version-derived time. Every drift sets `ledgerMismatch:true`; no up
logical call follows.

Up succeeds only with exit zero, no signal/timeout/truncation or prompt,
exactly one JSON object plus LF, exact keys `applied` and `message`, exact
message `Migrations applied`, exactly 112 canonical absolute isolated paths,
and stderr containing the connection line plus exactly 112 ordered
`Applying migration <basename>...` lines. Every drift maps to
`ledgerMismatch:true` and quarantine even if the real child exited zero.

The streaming scanner carries chunk overlap and maps known prompt tokens and
the JSON `Cannot prompt` error envelope to `promptDetected:true`. Unknown,
malformed, non-JSON, error, or excessive output independently quarantines, so
an unknown prompt can never be accepted. Target identity is never inferred
from success JSON because it contains no project ref.

## 8. Exact durable burn-ledger contract

### 8.1 Internally fixed location

The caller-supplied roots do not select the burn ledger. The adapter owns this
exact absolute constant:

```text
/Users/atsushinishikawa/Documents/Codex/2026-08-09/files-mentioned-by-the-user-dealeros/work/runtime/gda-estimate-postal-master-r5-cr6/burn-ledger-v1
```

Let `ledgerRoot` be that path, `projectDir` be
`ledgerRoot/nqvnjqcxgngqsqkbpdfi`, `activeLock` be
`projectDir/active.lock`, and `burnPath` be
`projectDir/<attemptId>.burn`.

Every path component is inspected with no-follow semantics. `ledgerRoot` and
`projectDir` are real directories owned by the effective uid, mode `0700`,
outside the repository, runtime tree, isolated migration tree, evidence
payload tree, and protected paths. No caller root may equal, contain, or be
contained by `ledgerRoot`. The fixed existing ancestor ending in
`work/runtime` supplies the expected `st_dev`; every created/used ledger
component must keep that same device identity through finalization. They cannot
be symlinks, aliases, mount-boundary substitutions, or caller-selected paths.
First creation uses mode `0700` and fsyncs the created directory and its
parent. Any ownership, mode, type, path, device, creation, or durability
uncertainty quarantines and launches no real process. `preflight-only` treats
an absent `ledgerRoot`/`projectDir` as an empty ledger and creates neither;
only separately approved `execute-once` may create them.

### 8.2 Canonical record

The exact UTF-8 burn bytes are:

```text
GDA_ESTIMATE_POSTAL_MASTER_R5_CR6_BURN_V1
project_ref=nqvnjqcxgngqsqkbpdfi
attempt_id=<attemptId>
state=BURNED_NO_RETRY
```

Every displayed line, including the last, ends in one LF. There is no BOM, CR,
JSON, timestamp, padding, blank line, or additional field. The safe attempt-id
grammar prevents separators or traversal.

Before preflight acceptance, enumerate `projectDir` without following links.
Apart from an optional validated `active.lock`, every entry must be a regular,
effective-uid-owned, mode-0600 `<safeAttemptId>.burn` file with exact canonical
content matching its filename and fixed project ref. Existing `burnPath`
means already burned. Any unknown, partial, empty, malformed, mismatched,
wrong-owner/mode/type, symlink, hard-link count other than one, read error, or
enumeration race fails closed as burned/ledger uncertainty; it is never
deleted, repaired, renamed, truncated, or ignored automatically.

### 8.3 Project execution lock

Immediately before durable burn, acquire `activeLock` with one atomic
no-replace `mkdir` at mode `0700`. Its exact owner record is a regular
mode-0600 file named `attempt` containing `<attemptId><LF>`, written and
fsynced before fsyncing `activeLock` and `projectDir`. Existing or uncertain
lock state launches no real process and requires a separately authorized
manual recovery; age never makes it stale.

The lock excludes different attempt ids from concurrent execution. Release is
permitted only after the child is confirmed exited, final evidence is
redacted/retained/read-back verified, and no additional process can launch.
Release removes the owner record, fsyncs the lock directory, removes the empty
lock directory, and fsyncs `projectDir`. Failure to release is retained as a
fail-closed stale lock and is never auto-recovered.

### 8.4 Burn-before-real-spawn transaction

After acquiring the lock and rechecking the directory, create `burnPath`
directly with `O_WRONLY|O_CREAT|O_EXCL|O_NOFOLLOW` and mode `0600`. Require a
new regular file owned by the effective uid with link count one. Write all
canonical bytes, reject short/extra writes, fsync the file, close it, reopen
and read back with no-follow semantics, compare exact bytes, then fsync
`projectDir`.

Only after every step succeeds may the first real OS list process launch. If
creation returns `EEXIST`, or any step after creation is partial/uncertain,
the attempt remains permanently burned and the real process is not launched.
Never unlink, replace, truncate, rewrite, or reuse a burn record. Evidence and
runtime cleanup must prove their targets are disjoint from `ledgerRoot`.

The validated burn records are converted to the internal `Set` that the
adapter supplies to frozen `isBurned`/`burnAttempt`; the public caller never
supplies it. The adapter does not weaken or replace the frozen pair-key
grammar. Preflight reads this reconstructed set but never writes it. Execute
rechecks it before locking and burning.

## 9. Exact synthetic-exit classification

Frozen `apply-once.mjs` remains unchanged. Its call to
`adapters.process.spawn()` is a **logical adapter call**, not proof that an OS
process was created. Every synthetic path preserves one logical
`spawn()`/one asynchronous `onExit()` cycle and launches zero real
OS/Supabase processes.

After frozen preflight returns an accepted plan, the outer adapter performs
target-identity validation before lock/burn:

1. if target identity is wrong, it acquires no lock and writes no burn record,
   then invokes frozen `applyOnce()` with the target-mismatch synthetic
   process adapter so the frozen core records `target_mismatch`;
2. if target identity is exact, it acquires the project lock, completes the
   durable burn transaction, and invokes frozen `applyOnce()`; and
3. the logical process adapter repeats the applicable checks synchronously
   immediately before each possible real launch to close time-of-check versus
   time-of-use drift.

### 9.1 Target-identity class

This class contains only:

- exact executable identity;
- complete argv and token positions;
- fixed project-ref token;
- resolved isolated workdir and cwd identities; and
- exact sanitized environment identity.

Any drift in this class returns a synthetic handle and the exact frozen
exitInfo:

```json
{
  "code": null,
  "signal": null,
  "promptDetected": false,
  "targetMismatch": true,
  "ledgerMismatch": false
}
```

The handle invokes no `node:child_process.spawn`, executable, shell, network,
or CLI. It accepts exactly one `onExit` callback and queues exactly one
asynchronous delivery. `terminate` and `waitForExit` are deterministic
no-process operations: `terminate()` resolves `undefined`, and
`waitForExit()` resolves the same exact exitInfo object without mutation or a
second callback. Neither throws. The frozen core returns `target_mismatch`.

### 9.2 Execution-readiness ledger class

This class contains only:

- exact durable burn-path existence, type, owner, mode, link count, device,
  canonical bytes, read-back result, and completed file/directory fsync;
- exact active-lock directory and owner-record existence, type, owner, mode,
  device, canonical attempt id, and continuing ownership by this invocation;
  and
- logical stage order: list is first; up is allowed only after this invocation's
  exact list success; neither stage may repeat.

Any loss, substitution, ambiguity, or drift in this class returns a synthetic
handle and the exact frozen exitInfo:

```json
{
  "code": null,
  "signal": null,
  "promptDetected": false,
  "targetMismatch": false,
  "ledgerMismatch": true
}
```

The handle has the same zero-real-process, one-callback asynchronous delivery,
and deterministic no-process terminate/wait behavior as Section 9.1. The
frozen core returns `ledger_mismatch`. The attempt remains burned; uncertain
lock state remains fail-closed.

A private adapter-only evidence subtype may be exactly one of
`BURN_RECORD_INVALID`, `ACTIVE_LOCK_INVALID`, or
`STAGE_ORDER_INVALID`. It is never added to frozen exitInfo, never affects
the frozen decision, and is retained only after redaction.

### 9.3 Deadline ownership

Executable deadline handling belongs exclusively to frozen
`apply-once.mjs/runWithTimeout` and its injected monotonic clock/timer.
Deadline is not target identity, is not ledger readiness, and cannot be mapped
to either synthetic mismatch class. The production process adapter performs
no long-running work: lock acquisition, durable burn, and first target check
finish before `applyOnce()`; the immediate pre-launch rechecks are bounded
synchronous identity/readiness checks only. A non-positive remaining duration
is rejected by frozen `runWithTimeout` before its logical spawn call, and an
active deadline uses the frozen timer/terminate/wait-for-confirmed-exit path.

### 9.4 Exact-target real launches

For an exact target and exact execution readiness, the list logical call may
invoke one real OS process only after Section 8's lock and durable burn
transaction. The up logical call may invoke at most one real process only
after exact list success and while the same lock remains owned. No other
stage, fallback, direct SQL, retry, repair, reset, DB URL, or manual
continuation exists.

Tests and retained evidence count logical adapter calls and real OS launches
separately. Target mismatch, burn invalidity, lock invalidity, and stage-order
invalidity each prove one logical call, one exact recognized frozen outcome,
and zero real launches.

## 10. Evidence and no-retry boundary

- Import has zero filesystem, Git, process, environment, network, Supabase, or
  database effects.
- Raw child bytes are volatile and never printed or retained.
- Retained evidence contains only redacted status, byte counts/hashes,
  normalized flags, child exit data, logical-call and real-launch counts, burn
  path identity hash, lock lifecycle, and final verification data.
- Evidence contains no credential, URL, header, environment dump, raw stream,
  protected content, or burn-record content.
- Finalize by redact, retain, read back, rescan, and verify SHA-256 before raw
  deletion and lock release.
- A burned attempt stays burned on success, quarantine, crash, signal, timeout,
  evidence failure, or lock-release failure.
- No automatic retry, stale-lock removal, record repair, or attempt-id reuse is
  permitted.

## 11. Future implementation allowlist

A later explicit Owner authorization may create exactly:

1. `scripts/e2e/gda-estimate-postal-master-r5-cr6/hosted-execution-adapter.mjs`
2. `scripts/e2e/gda-estimate-postal-master-r5-cr6/hosted-execution-adapter.test.mjs`

No existing harness, source, test, migration, configuration, package,
lockfile, or governance document may change during implementation.

## 12. Mandatory offline hostile tests

All tests use fake Git, filesystem, process, clock, timer, ledger, deletion,
retention, and CLI adapters. They prove at minimum:

1. zero import effects and exact public input shape;
2. exact attempt grammar including 1/128 acceptance and dot/129 rejection;
3. exact Git acquisition, protected metadata, 111/112 batches, aggregate,
   both-mode materialization, and independent actual evidence;
4. preflight performs no lock, burn, linked-command construction, hosted
   contact, or retention and deletes only its isolated local tree;
5. ledger root is internally fixed and caller-unoverrideable;
6. path-component symlink, wrong owner/mode/type/device, hard link, unknown
   entry, malformed/partial record, enumeration race, and read/fsync failure
   all prevent real launch and preserve fail-closed state;
7. same-attempt races yield exactly one exclusive burn winner; different
   attempts cannot execute concurrently because exactly one active lock wins;
8. crash/failure at every lock and burn step never permits a real launch
   before durable read-back and directory fsync;
9. existing burn or active lock is never auto-deleted, repaired, aged out, or
   reused;
10. target-identity drift yields the exact `targetMismatch:true` synthetic
    exitInfo, one logical call, zero real launches, and target quarantine;
11. burn, lock, and stage-order drift each yield the exact
    `ledgerMismatch:true` synthetic exitInfo, one logical call, zero real
    launches, the exact private subtype, and ledger quarantine;
12. deadline is never classified as target/ledger mismatch and follows only
    frozen non-positive/timer/terminate/wait behavior;
13. exact list/up byte mapping, prompt chunk splits, malformed/error JSON,
    stderr drift, migration-ledger drift, timeout/signal/non-zero and limits
    quarantine;
14. exact fake success runs one logical/real list and at most one logical/real
    up under one lock and one durable burn;
15. evidence finalization precedes lock release and cleanup cannot target the
    ledger; and
16. all existing seven CR6 test files pass unchanged.

## 13. Future diagnosis and results

After separate three-document commit and normal-push approvals, one
tool-disabled read-only diagnosis may receive exactly `AGENTS.md`, this
directive, completion plan, phase ledger, and the fourteen harness files: 18
files total. Protected paths remain metadata-only. No tools, edits, tests, Git,
Supabase, DB/provider, network, or mutations.

Required diagnosis marker:

`GDA_ESTIMATE_WIZARD_POSTAL_MASTER_R5_CR6_R3F_READ_ONLY_DIAGNOSIS_RESULT_V1`

Allowed verdicts:

- `READY_FOR_TWO_FILE_OFFLINE_IMPLEMENTATION`
- `CHANGES_REQUIRED_DIRECTIVE`
- `BLOCKED_INPUT`

Codex independently accepts the diagnosis before implementation. A later
implementation result uses marker
`GDA_ESTIMATE_WIZARD_POSTAL_MASTER_R5_CR6_R3_HOSTED_EXECUTION_ADAPTER_IMPLEMENTATION_RESULT_V1`
and may return only `READY_FOR_CODEX_OFFLINE_ACCEPTANCE`,
`CHANGES_REQUIRED_SOURCE`, `CHANGES_REQUIRED_CLI_COMPATIBILITY`, or
`BLOCKED_ENVIRONMENT`.

Offline implementation verification includes syntax checks for all sixteen
files, one test run covering the new test plus existing seven tests, exact
two-path/mode checks, unchanged fourteen-file hash, protected metadata-only
checks, clean index/upstream, and `git diff --check`. It contacts no hosted
system.

## 14. Stop rule

Stop after authoring and independently checking this exact three-document
candidate. Do not stage, commit, push, invoke Claude, retransmit private files,
implement/test adapters, execute Git or Supabase CLI for implementation,
contact a hosted project/database/provider, replay migrations, upload
evidence, mark Ready, merge, deploy, cut over, or retire anything. Every later
boundary requires separate Owner authorization.
