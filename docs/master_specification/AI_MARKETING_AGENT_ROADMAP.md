# AI Marketing Agent — Future Roadmap
## GYEON Detailer Agent: From Business Management to AI Growth Platform

| Field | Value |
|-------|-------|
| **Document** | AI Marketing Agent Roadmap |
| **Status** | Approved Future Feature — Deferred |
| **Created** | 2026-06-26 |
| **Priority** | Future Version (after core platform reaches stable production) |
| **Phases** | PHASE 71 – PHASE 76 |
| **Prerequisite** | AI Gateway Architecture (`AI_GATEWAY_SPEC.md`) must be implemented first |
| **Implementation** | Do not implement. Specification and roadmap only. |

> **Architecture update (2026-06-26):** PHASE 77 (AI Review Request Agent) has been moved to the **AI Reputation Agent** track (`AI_REPUTATION_AGENT_ROADMAP.md`). Reputation management is now a separate Pro+ module. This document covers PHASE 71–76 (AI Marketing + AI Growth) only.

---

## Vision

Transform GYEON Detailer Agent from a business management system into an **AI business growth platform**.

The core platform manages operations: estimates, invoices, work orders, customers, vehicles, reminders.
The AI Marketing Agent layer adds a new capability: automatically turning completed work into marketing content — helping dealers grow their business without additional effort.

**Long-term goal:** An autonomous business assistant that helps dealers increase sales, customer retention, and marketing efficiency — not just manage operations.

---

## Principles

1. **Dealer always approves.** Automatic publishing must remain optional. Dealer review and approval before any public post is the default behavior.
2. **Privacy-first.** License plates and faces must be blurred before any image leaves the system.
3. **No extra work for the dealer.** The agent runs automatically from existing job data — dealers do not need to produce content manually.
4. **Platform-specific content.** Each social platform receives content optimized for its format and audience.
5. **AI recommendations, human decisions.** The Growth Agent suggests; the dealer decides.
6. **Discovery-optimized by default.** All generated content must be structured for discovery across Google search, SNS search, Google Maps, and AI answer engines. SEO, MEO, AEO, LLMO, and AIO are mandatory considerations in every piece of content — not optional enhancements.

---

## Discovery Optimization — Mandatory Requirement

> **Status:** Mandatory future requirement. Applies to all AI-generated content across PHASE 73, 74, 75, and 76.
> Not optional. Not an enhancement. Every content generation pipeline must satisfy this requirement.

All SNS posts and marketing content generated by the AI Marketing Agent must be simultaneously optimized for five discovery channels:

### Optimization Targets

| Abbreviation | Full Name | What it means for this product |
|-------------|-----------|-------------------------------|
| **SEO** | Search Engine Optimization | Content indexed and ranked by Google, Bing, and SNS search. Requires service keywords, vehicle/model keywords, GYEON product keywords, structured captions. |
| **MEO** | Map Engine Optimization | Google Business Profile visibility for local search ("近くのカーコーティング"). Requires local area keywords, city/region names, geo-tagged content, structured location signals. |
| **AEO** | Answer Engine Optimization | Featured snippets and "Answer Box" results in Google search. Requires FAQ-style answer content, question-and-answer structure, concise factual statements about services. |
| **LLMO** | Large Language Model Optimization | Content that LLMs (ChatGPT, Gemini, Claude, etc.) cite when answering questions about local auto detailing. Requires structured, entity-rich, authoritative text that LLMs can extract and reference. |
| **AIO** | AI Overview Optimization | Google AI Overviews (formerly SGE). Requires concise summaries, structured answers, factual product/service descriptions that Google's AI can surface in overview panels. |

### Mandatory Content Elements Per Post

Every generated post must include or consider the following elements, applied according to platform constraints:

| Element | Purpose | Example |
|---------|---------|---------|
| Local area keyword | MEO — attract local search | `渋谷区カーコーティング`, `名古屋市ガラスコーティング` |
| Service keyword | SEO/MEO — identify the service | `セラミックコーティング施工`, `PPFフィルム施工` |
| Vehicle/model keyword | SEO — vehicle-specific searches | `BMW M3 コーティング`, `アルファード PPF` |
| GYEON product keyword | SEO/LLMO — brand authority | `GYEON Mohs+ 施工`, `GYEON One コーティング` |
| Search-friendly caption | SEO/AEO — indexable natural language | Full sentences describing the job, not just hashtags |
| Structured answer content | AEO/AIO — Q&A format when appropriate | "GYEONコーティングの耐久性は？ → 約2〜5年です。" |
| AI-search-friendly text | LLMO/AIO — extractable by AI engines | Entity names, durations, product specs in plain text |
| Hashtag optimization | SNS search — platform-specific discovery | Quantity, relevance, and reach tuned per platform |
| Platform metadata | Platform SEO — titles, alt text, descriptions | Instagram alt text, YouTube title keywords, TikTok description |
| FAQ snippet (where appropriate) | AEO — trigger featured snippet / AI answer | Short Q&A block appended to caption for long-form platforms |

### Platform-Specific Application

| Platform | Priority discovery channels | Key requirements |
|---------|----------------------------|-----------------|
| Instagram | SNS search, SEO (via Google indexing) | First 125 chars of caption are critical; alt text on images; location tag |
| Facebook | SEO (Google indexes public posts), MEO | Full sentences; location check-in; Google Business integration signals |
| X (Twitter) | SNS search, AIO | Concise structured statements; entity names spelled out |
| TikTok | SNS search, SEO (growing) | First line of description; on-screen text keywords; caption keywords |
| YouTube Shorts | SEO, AIO, LLMO | Title keyword; description with structured sentences; chapters if long enough |
| LINE VOOM | LINE search | Japanese-first; service and location keywords |
| Google Business Profile posts | MEO, AEO, AIO | Highest priority for local discovery; structured service descriptions; FAQ content |

> Google Business Profile is not listed in PHASE 74's social platforms but should be considered as a primary MEO publishing target in a future sub-phase.

### Content Quality Requirement

Captions generated by PHASE 73 must not be hashtag-only or emoji-only. Every post must contain at least one full sentence in natural Japanese that describes:
- What service was performed
- What product was used (GYEON product name)
- Where the shop is located (city/region — from `dealer_settings`)
- Vehicle type or model (from work order)

This sentence structure serves both human readability and AI engine extractability.

---

## PHASE 71 — AI Media Management Foundation

**Prerequisite:** Core business platform at stable production.
**Sprint 10I:** Media-first architecture types defined. `src/lib/media/media-types.ts` created.

> **Architecture note:** The system supports both photos and videos from the start.
> Use `MediaType = "photo" | "video"` — not "image" — throughout all new code.
> See `MEDIA_ARCHITECTURE.md` for the canonical media model and migration plan.

### 71.1 Job Media Management

| Feature | MediaType | Description |
|---------|-----------|-------------|
| Before/after photo uploads | photo | Attach before and after photos to completed work orders |
| Before/after video uploads | video | Short video clips from job start and completion |
| Work-in-progress documentation | photo / video | Mid-job captures linked to work order timeline |
| Water beading video | video | Coating effectiveness demonstration |
| Completion report media gallery | photo | Per-phase grid in the completion report |
| Dealer media library | photo + video | Global library across all jobs, searchable and filterable |

### 71.2 AI Media Processing

| Feature | MediaType | Description |
|---------|-----------|-------------|
| AI photo quality evaluation | photo | Automatic scoring: sharpness, lighting, composition |
| AI video quality evaluation | video | Blur detection, stability scoring, resolution check |
| Automatic best-shot selection | photo | AI picks the strongest before/after pair from a batch |
| Automatic license plate blur | photo + video | Server-side before any media is stored or published |
| Automatic face blur | photo + video | Server-side before any media is stored or published |
| Video thumbnail generation | video | First-frame JPEG, stored at `thumbnails/{media_id}.jpg` |

**Security rule:** License plate blur and face blur run server-side before any media is stored.
No unblurred media with identifiable information may be stored in dealer-accessible storage
or transmitted to any external AI service.

### 71.3 Customer Consent for Media

All media defaults to `visibility = "internal_only"`. Escalating to customer-visible or marketing-approved requires explicit consent.

| Step | Action | Result |
|------|--------|--------|
| Upload | Dealer uploads photo or video | `visibility = "internal_only"` |
| Completion report | Dealer marks file as `is_public = true` | `visibility = "customer_visible"` |
| Marketing consent | Dealer sends consent request to customer | `consent_status = "pending"` |
| Customer approves | Customer confirms via LINE or web link | `consent_status = "approved"` |
| Marketing approval | Dealer marks file | `visibility = "marketing_approved"`, `is_marketing_approved = true` |

### 71.4 Dealer Branding Assets

| Feature | Description |
|---------|-------------|
| Dealer logo management | Upload and manage shop logo and brand assets |
| Brand color settings | Primary and secondary brand colors for content overlays |
| Brand templates | Pre-designed overlay templates for before/after photos and videos |
| Watermark settings | Optional shop name watermark position and opacity |
| Brand preview | Preview how branded content will appear |

### Data model additions (Phase 10J migration — CTO approval required)

```sql
-- Extend work_order_files (preferred over new table — reuses existing upload pipeline)
ALTER TABLE work_order_files
  ADD COLUMN thumbnail_path        text,
  ADD COLUMN duration_seconds      float,
  ADD COLUMN width                 integer,
  ADD COLUMN height                integer,
  ADD COLUMN captured_at           timestamptz,
  ADD COLUMN uploaded_by           uuid REFERENCES auth.users(id),
  ADD COLUMN completion_report_id  uuid REFERENCES completion_reports(id),
  ADD COLUMN consent_status        text NOT NULL DEFAULT 'not_required',
  ADD COLUMN visibility            text NOT NULL DEFAULT 'internal_only',
  ADD COLUMN is_marketing_approved   boolean NOT NULL DEFAULT false,
  ADD COLUMN is_ai_training_excluded boolean NOT NULL DEFAULT false;

-- Dealer branding assets (new table)
CREATE TABLE dealer_brand_assets (
  id                  uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  dealer_id           uuid NOT NULL REFERENCES dealers(id),
  logo_storage_path   text,
  primary_color       text,
  secondary_color     text,
  watermark_enabled   boolean NOT NULL DEFAULT false,
  watermark_position  text,
  watermark_opacity   float,
  template_id         text,
  created_at          timestamptz NOT NULL DEFAULT now(),
  updated_at          timestamptz NOT NULL DEFAULT now()
);
```

---

## PHASE 72 — AI Video Generator

**Prerequisite:** PHASE 71 complete. Media library populated.

### 72.1 Automatic Video Creation

The system automatically creates marketing videos from completed job data — no editing required from the dealer.

| Input | Source |
|-------|--------|
| Before / after images | `work_order_media` |
| Job details | Work order: service category, product used, vehicle |
| Dealer branding | `dealer_brand_assets` |
| Background music | Licensed library (curated by service) |

### 72.2 Output Formats

| Format | Duration | Platform |
|--------|----------|----------|
| Instagram Reel | 15s / 30s | Instagram |
| TikTok | 15s / 30s | TikTok |
| YouTube Shorts | 60s | YouTube |
| Facebook Reel | 30s | Facebook |
| LINE VOOM | 30s | LINE |

All formats generated from the same source media — platform-specific aspect ratio and duration applied automatically.

### 72.3 Video Customization

- Transition style selection (fade, cut, zoom)
- Text overlay position and font
- Brand color theme applied automatically
- Music volume / no-music option
- Preview before publishing

### 72.4 Generated Video Retention

AI-generated videos are **not stored permanently by default**.

| Event | Default behavior | Dealer-configurable |
|-------|-----------------|---------------------|
| Generation | Retain for 30 days | Yes |
| Dealer download confirmed | Keep for remainder of 30-day window | Delete immediately (opt-in) |
| SNS publish confirmed | Keep for remainder of 30-day window | Delete immediately (opt-in) |
| 30-day window expires | Delete file — keep `MediaDeletionRecord` | Change period: 7 / 30 / 90 days |

**After file deletion**, only the following metadata is kept:
- `media_id` — unique asset identifier
- `work_order_id` — the job this video was generated for
- `customer_id` — for privacy audit
- `vehicle_id`
- `deleted_at` — timestamp of physical deletion
- `download_status` — whether the dealer downloaded the file
- `publish_status` — whether it was published to SNS
- `generated_output_record` — ID linking to the source media used as input

**Source video retention:**
If the dealer's source footage was used as input for AI generation, the source video follows its own retention policy independently. By default, source videos are also deleted after 30 days. Dealers may opt in to delete source footage immediately after AI processing completes.

**Implementation:**
- Retention types: `VideoRetentionPolicy`, `VideoGeneratedRetentionConfig`, `VideoRetentionPeriod` in `src/lib/media/media-video.ts`
- Deletion enforcement runtime: **NOT IMPLEMENTED** — Phase 10K, requires CTO approval
- Dealer settings UI: **NOT IMPLEMENTED** — Phase 10K

---

## PHASE 73 — AI Content Writer

**Prerequisite:** PHASE 71 complete. Job metadata available.

### 73.1 Generated Content Types

| Content | Discovery channel | Description |
|---------|------------------|-------------|
| Post title | SEO, AIO | Attention-grabbing headline; includes primary service keyword and location |
| Caption (main body) | SEO, AEO, LLMO | 1–3 paragraphs; natural Japanese sentences; must include service, product, vehicle, location |
| Search-friendly caption layer | SEO, AEO, AIO | Structured plain-language version optimized for indexing; no emojis or abbreviations |
| AI-answer-friendly text | AEO, LLMO, AIO | Factual statements about service benefits, product specs, durability — structured for AI extraction |
| FAQ snippet (optional per platform) | AEO, AIO | Short Q&A block; e.g. "GYEONコーティングはどのくらい持ちますか？→ 約2〜5年" |
| Local area keywords | MEO, SEO | City/ward/neighborhood names; geo-specific service terms |
| Service keywords | SEO, MEO | Specific service name in Japanese; matches common search queries |
| Vehicle / model keywords | SEO | Make, model, grade of vehicle serviced — high-intent search terms |
| GYEON product keywords | SEO, LLMO | Official GYEON product name + grade; increases brand entity authority in LLMs |
| Hashtags | SNS search | Platform-quantity-tuned; mix of broad, niche, and location tags |
| Platform-specific metadata | Platform SEO | Instagram alt text; YouTube title/description; TikTok first-line optimization |
| Dealer branding voice | — | Content tone adapted to dealer's business name and style |

### 73.2 Content Context Inputs

The AI writer uses the following job data automatically:

- Service category (coating / PPF / window film / maintenance / carwash / room cleaning / other)
- GYEON product used (from estimate line items)
- Vehicle make and model
- Body size tier
- Dealer name and location (from `dealer_settings`)
- Before/after image quality score (from PHASE 71)

### 73.3 Content Safety

- No AI-generated content contains customer PII
- No AI-generated content references customer names, addresses, or contact details
- Vehicle plate numbers are never included in generated text (blurred in images, omitted in text)

### 73.4 Discovery Optimization Engine

This is a mandatory component of PHASE 73. Every content generation run must pass through the Discovery Optimization Engine before the draft is made available to the dealer.

**Input: job data assembled by the engine**

```
dealer_settings.prefecture + city    → MEO location signal
dealer_settings.name                 → brand entity
work_order.service_category          → primary service keyword
estimate line items: GYEON product   → product keyword (official name from canonical catalog)
vehicle.make + model + grade         → vehicle keyword
dealer_settings.detailer_rank        → enables certified-product keywords if 'certified'
```

**Engine output: keyword manifest (per post)**

```typescript
type DiscoveryKeywordManifest = {
  meo_primary:     string;   // e.g. "渋谷区カーコーティング"
  meo_secondary:   string;   // e.g. "東京都セラミックコーティング"
  seo_service:     string;   // e.g. "GYEONセラミックコーティング施工"
  seo_vehicle:     string;   // e.g. "BMW 3シリーズ コーティング"
  seo_product:     string;   // e.g. "GYEON Mohs+ 施工"
  aeo_statement:   string;   // e.g. "GYEONコーティングは約2〜5年の耐久性があります"
  aeo_faq:         { q: string; a: string } | null;  // optional FAQ pair
  llmo_entity:     string;   // e.g. "GYEON Japanの認定ディテイラー施工"
  hashtags:        Record<"instagram" | "tiktok" | "youtube" | "x" | "facebook" | "line_voom", string[]>;
}
```

**Caption structure (minimum required)**

Every generated caption must contain, in order:

1. **Hook sentence** — service + vehicle, in natural Japanese
   > 「{make}{model}にGYEON {product}を施工しました。」
2. **Location signal** — shop city/region
   > 「{city}でセラミックコーティングをお探しの方はぜひご相談ください。」
3. **Product/benefit statement** — used for AEO/LLMO extraction
   > 「{product}は{benefit}の特徴があり、{durability}程度の耐久性が期待できます。」
4. **Call to action** — booking or contact
   > 「ご予約・お問い合わせは {contact_method} から。」
5. **FAQ block** (optional, long-form platforms only)
   > 「Q: {common_question} / A: {concise_answer}」
6. **Hashtags** — platform-tuned quantity and mix

**Hashtag strategy per platform**

| Platform | Count | Composition |
|---------|-------|-------------|
| Instagram | 20–30 | 5 broad (コーティング), 5 service-specific, 5 vehicle, 5 location, 5 GYEON brand |
| TikTok | 5–8 | High-reach trending + service + location |
| YouTube Shorts | 3–5 | High-volume search terms only |
| X | 2–3 | Concise; avoid hashtag stuffing |
| Facebook | 5–10 | Service + location + brand |
| LINE VOOM | 3–5 | Japanese-specific; location + service |

---

## PHASE 74 — AI Social Publishing

**Prerequisite:** PHASE 72 and PHASE 73 complete. Dealer approves generated content.

### 74.1 Supported Platforms

| Platform | Content types |
|---------|---------------|
| Instagram | Images, Reels, Stories |
| Facebook | Images, Reels, Posts |
| X (Twitter) | Images, short-form posts |
| TikTok | Videos |
| YouTube Shorts | Videos |
| LINE VOOM | Videos, images |

### 74.2 Publishing Workflow

```
Job completed
  → AI generates media + content draft
  → Dealer receives notification: "新しいコンテンツが準備できました"
  → Dealer reviews draft in GYEON Detailer Agent
  → Dealer edits title / caption / hashtags (optional)
  → Dealer selects target platforms
  → Dealer approves → scheduled or immediate publish
  → System publishes to connected platform APIs
  → Publish confirmation and URL stored in job record
```

**Default behavior: dealer approval required before any publish.**
Automatic publishing without dealer review must remain an opt-in setting, never the default.

### 74.3 Scheduling Features

| Feature | Description |
|---------|-------------|
| Immediate publish | Post now to selected platforms |
| Scheduled publish | Set a specific date and time |
| AI-recommended timing | AI suggests optimal posting time based on platform engagement data |
| Recurring schedule | Auto-schedule new content at regular intervals |
| Queue management | View, reorder, or cancel scheduled posts |

### 74.4 Platform-Specific Optimization

| Platform | Optimization applied |
|---------|---------------------|
| Instagram | Square / portrait aspect ratio; 30-hashtag limit; geotag support |
| TikTok | Vertical 9:16 video; trending sound suggestions |
| YouTube Shorts | 60s max; chapter markers; description keywords |
| X | 280-character limit; link preview handling |
| LINE VOOM | Japanese text preferred; LINE-specific emoji |

### 74.5 Platform Connection

- OAuth2 connection per platform (dealer connects their account)
- Credentials stored encrypted, server-side only — never in client state
- Revokable at any time from dealer settings
- One dealer account per platform connection

---

## PHASE 75 — AI Marketing Analytics

**Prerequisite:** PHASE 74 active. Published posts accumulating data.

### 75.1 Performance Metrics

| Metric | Source |
|--------|--------|
| Reach | Platform API |
| Views | Platform API |
| Saves / bookmarks | Platform API |
| Likes | Platform API |
| Shares / reposts | Platform API |
| Comments | Platform API |
| Profile visits from post | Platform API |
| Click-through rate | Platform API (if link included) |

### 75.2 Business-Level Analytics

| Insight | Description |
|---------|-------------|
| Popular services | Which service categories generate the most engagement |
| Best-performing images | Which before/after pairs drive the most saves/reach |
| Best posting times | When engagement peaks by platform and day of week |
| Growth trends | Follower growth and reach growth over time |
| Top-performing posts | Ranked list with key metrics |
| Estimated new customer inquiries | Correlation between post reach and new bookings (if booking data available) |

### 75.3 Discovery Performance Analytics

Tracks how well generated content performs across SEO, MEO, AEO, LLMO, and AIO channels. This is a mandatory analytics category, not optional.

| Metric | Channel | Source |
|--------|---------|--------|
| Google Business Profile post views | MEO | GBP Insights API |
| GBP post call / direction clicks | MEO | GBP Insights API |
| Local search impressions | MEO, SEO | GBP Insights API / Google Search Console |
| Google organic impressions (for posts indexed) | SEO | Google Search Console (if connected) |
| Top-performing service keywords | SEO, MEO | Aggregated from content performance data |
| Top-performing location keywords | MEO | Aggregated from content performance data |
| AI search citation count (estimated) | LLMO, AIO | Detectable via referral traffic from AI engines where available |
| FAQ snippet trigger rate | AEO | Manual or Search Console data when available |
| Keyword velocity | SEO | New high-performing keywords discovered over time |

**Note on LLMO/AIO tracking:** AI engine citations are not currently measurable via standard APIs. The system should track referral traffic from known AI engine domains (e.g., `perplexity.ai`, `chat.openai.com`, `gemini.google.com`) as a proxy signal when those sources appear in analytics.

### 75.4 Discovery AI Recommendations

In addition to engagement recommendations (§75.3), the AI generates discovery-focused recommendations:

- 「"{city}カーコーティング" のキーワードを含む投稿のリーチが他より{n}倍高くなっています」
- 「GYEON製品名を本文に含めた投稿はAI検索での引用数が増加しています」
- 「Instagram投稿にFAQ形式のテキストを追加すると、Google検索表示回数が向上する傾向があります」
- 「{vehicle_model}のキーワードを追加することで車種別検索流入が見込まれます」

### 75.5 AI Recommendations (Engagement)

After analyzing historical engagement performance, the AI generates actionable recommendations:

- "ボディコーティングの施工動画は土曜の19時前後に投稿すると閲覧数が最も高くなっています"
- "PPF施工のビフォーアフターは他のカテゴリより3倍保存されています"
- "ハッシュタグ #セラミックコーティング を追加したポストはリーチが平均42%増加しています"

Recommendations are displayed in Japanese and tailored to the specific dealer's history.

---

## PHASE 76 — AI Growth Agent

**Prerequisite:** PHASE 71–75 operational. Sufficient historical data accumulated.

### 76.1 Proactive Detection

The AI Growth Agent monitors dealer activity and proactively identifies opportunities:

| Trigger condition | Agent action |
|------------------|-------------|
| SNS account connected but no post in 14 days | Alert + suggest content from recent jobs |
| 3+ completed jobs with no media uploaded | Prompt dealer to add before/after photos |
| High-quality job media available but no post drafted | Auto-generate draft and notify dealer |
| Seasonal opportunity detected (spring, rainy season, summer heat) | Suggest maintenance campaign content |
| Competitor posting frequency increases (if detectable) | Recommend increased posting cadence |
| Customer approaching maintenance reminder date | Suggest re-engagement post (anonymized) |

### 76.2 Automated Content Pipeline

When the dealer opts in to the automated pipeline:

```
Completed work order
  → AI evaluates media quality
  → If quality score ≥ threshold:
      AI generates video (PHASE 72)
      AI writes caption + hashtags (PHASE 73)
      Draft enters approval queue
      Dealer notified: "1件のコンテンツが自動作成されました"
  → Dealer approves or edits → publish
```

**The dealer always has a review step.** Automatic publish without approval is never triggered unless the dealer has explicitly enabled a "fully automated" mode (which requires a separate opt-in confirmation screen).

### 76.3 Campaign Ideas

The AI proactively generates marketing campaign concepts:

| Campaign type | Example |
|--------------|---------|
| Seasonal | "梅雨前のコーティングキャンペーン" — rainy season prep |
| Service spotlight | "PPF施工特集" — feature a service category for a week |
| Before/after series | "施工事例ウィーク" — one job featured per day |
| Customer milestone | "1,000台施工達成" — milestone celebration post |
| Product launch | New GYEON product tied to a recent job |

### 76.4 Growth Dashboard

A dedicated dashboard section showing:

- Content pipeline status (drafts pending, scheduled, published)
- Growth velocity (follower growth rate, reach trend)
- Revenue attribution (estimated job inquiries from social content)
- AI agent activity log (what the agent noticed and suggested)
- Campaign performance summary

---

## PHASE 77 — AI Review Request Agent [MOVED]

> **This phase has been moved to the AI Reputation Agent track.**
> See `AI_REPUTATION_AGENT_ROADMAP.md` PHASE 77 for the full specification.

PHASE 77 (AI Review Request Agent) is now part of the **AI Reputation Agent** — a separate Pro+ module. Reputation management (review collection, Google Business Profile integration, reputation analytics) is distinct from marketing content creation and social publishing.

**Reason for separation:** Review collection involves strict compliance rules, legal considerations, and a different workflow from content marketing. Keeping it as a separate module makes the compliance requirements clearer and easier to enforce independently.

For the complete PHASE 77 specification, see: `AI_REPUTATION_AGENT_ROADMAP.md`

---

## [Former PHASE 77 content — now in AI_REPUTATION_AGENT_ROADMAP.md]

> *This section intentionally left as a redirect. The full PHASE 77 specification is in `AI_REPUTATION_AGENT_ROADMAP.md`. Do not reconstruct PHASE 77 content here.*

---

## Phase Dependency Map

```
AI Gateway Architecture (prerequisite — see AI_GATEWAY_SPEC.md)
  │
  ▼
PHASE 71 — AI Media Foundation
  (before/after images, AI blur, branding assets)
      │
      ├── PHASE 72 — AI Video Generator
      │       (requires: job media)
      │
      ├── PHASE 73 — AI Content Writer + Discovery Optimization Engine
      │       (requires: job metadata + media quality score)
      │
      └─────────────────────┐
                            ▼
                   PHASE 74 — AI Social Publishing
                        (requires: PHASE 72 + 73)
                            │
                            ▼
                   PHASE 75 — AI Marketing Analytics
                        (requires: published post data)
                            │
                            ▼
                   PHASE 76 — AI Growth Agent
                        (requires: sufficient history)
```

> **AI Reputation Agent (PHASE 77–81)** is a separate track.
> See `AI_REPUTATION_AGENT_ROADMAP.md` for the full specification.

---

## [PHASE 77 moved]

> PHASE 77 has been moved to `AI_REPUTATION_AGENT_ROADMAP.md`. See that document for the complete specification.

---

## AI Gateway Dependency

All AI inference calls in PHASE 71–76 route through the **AI Gateway** (`AI_GATEWAY_SPEC.md`).

| Principle | Impact on this track |
|-----------|---------------------|
| Provider-agnostic | No specific AI provider is hardcoded. Content writing, image analysis, video generation all route through the gateway adapter. |
| Dealer-owned keys | Dealers configure their own AI provider API keys in `dealer_ai_settings`. Office AZ does not pay inference costs. |
| Server-side only | API keys are never returned to client. All AI calls are server-side. |

**Implementation prerequisite:** AI Gateway must be implemented before any PHASE 71–76 feature is built.

---

## Technology Considerations

| Capability | Candidate technology (via AI Gateway) |
|------------|--------------------------------------|
| License plate / face blur | OpenAI Vision API or dedicated CV model (dealer's key) |
| AI image quality scoring | OpenAI Vision API or Gemini (dealer's key) |
| AI video generation | Runway, Sora API, or similar when available (separate key management TBD) |
| AI content writing | Claude or OpenAI (dealer's key via gateway) |
| Social publishing | Instagram Graph API, Facebook API, TikTok API, YouTube Data API, LINE Messaging API |
| Analytics ingestion | Platform Insights APIs |
| Background processing | Supabase Edge Functions or dedicated job queue |

**All AI API keys must be stored server-side. No AI API credentials may be exposed to the client.**

---

## Deferred — What Is Explicitly Out of Scope

| Feature | Why deferred |
|---------|-------------|
| Fully autonomous posting (no dealer review) | Dealer control is a core principle — may be opt-in later |
| Customer-facing content (posting customer names, reviews) | Privacy; requires separate consent flow |
| Paid advertising management (Meta Ads, Google Ads) | Separate product scope; requires billing integration |
| Competitor analysis (monitoring specific competitor accounts) | Legal and ethical considerations |
| AI-generated before images (synthetic) | Could mislead customers — permanently out of scope |

---

## Implementation Gate

**Do not begin any PHASE 71–76 implementation until:**

1. AI Gateway Architecture implemented and validated (`AI_GATEWAY_SPEC.md`)
2. Core business platform (estimates, invoices, work orders, customers, LINE) is at stable production
3. Sprint 10 (dealer approval flow) is complete
4. CTO sign-off on media storage architecture and AI vendor selection
5. Privacy policy updated for AI image processing and social platform connections
6. Separate product specification pass for each phase under SDD

---

---

## Sprint 11A — AI Marketing Platform Foundation

> Full specification: `MARKETING_PLATFORM_SPEC.md`

Sprint 11A implements the architecture-only foundation for the AI Marketing Platform.
No API integrations, no AI inference, no database migrations.

### What was implemented

| Module | File | Purpose |
|--------|------|---------|
| Marketing Domain | `marketing-types.ts` | MarketingCampaign, MarketingPolicy, MarketingAudience, MarketingSchedule, 10 asset types, 10 channel IDs |
| Marketing Asset | `marketing-asset.ts` | MarketingAsset (consumes MediaAsset), permission checks, publishability gate |
| Channel Registry | `marketing-channel.ts` | MARKETING_CHANNEL_REGISTRY — 10 channels with full capability metadata |
| Publishing Workflow | `marketing-workflow.ts` | 9-stage state machine, WORKFLOW_TRANSITIONS, deriveWorkflowStage() |
| Optimization Model | `marketing-optimization.ts` | SEO + MEO + AEO + LLMO + AIO metadata profiles |
| AI Compatibility | `marketing-ai.ts` | MarketingAssetForAgent, toMarketingAssetForAgent(), AGENT_MARKETING_CAPABILITIES |
| Public API | `index.ts` | Re-exports from all marketing modules |

### Key design decisions

- `MarketingAsset.source_media: MediaAsset[]` — marketing content is always grounded in canonical media domain objects
- All channels have `available_now = false` — Phase 11B will flip individual channels one at a time
- `DEFAULT_MARKETING_POLICY.requires_dealer_approval = true` — dealers always approve before publishing
- `DEFAULT_MARKETING_POLICY.ai_content_allowed = false` — must be explicitly enabled per campaign
- LLMO and AIO targets added alongside SEO/MEO/AEO — forward-looking for next 3–5 years of search evolution
- `toMarketingAssetForAgent()` runs a full media permission gate before projecting to `MarketingAssetForAgent`

### Next steps (Sprint 11B)

1. Implement Instagram Feed + Google Business Profile channel adapters
2. Implement `marketing_agent` caption and hashtag generation (uses AI Gateway)
3. Implement dealer approval UI for MarketingAsset
4. Implement `seo_agent` optimization pass for ContentOptimizationProfile
5. Implement `MediaAssociationService` (requires media_assets migration approval)

---

## Sprint 11B — AI Video Pipeline Foundation

### Completed: 2026-06-26

Sprint 11B implements the production-ready AI Video Pipeline foundation as a new business domain at `src/lib/video/`. This is the canonical architecture for all future AI-generated marketing videos in the GYEON Detailer Agent platform.

### Architecture Overview

```
MediaAsset (src/lib/media)
  ↓ wrapped by
VideoSource (video-source.ts)
  ↓ sequenced into
VideoTimeline / VideoScene (video-timeline.ts)
  ↓ assembled into
VideoProject (video-project.ts)
  ↓ targeted at
VIDEO_PUBLISHING_PROFILE_REGISTRY (video-publishing.ts) — 9 profiles
  ↓ generated by
VIDEO_AI_PROVIDER_REGISTRY (video-ai.ts) — 8 providers (all pending integration)
  ↓ governed by
VideoPrivacyConfig (video-privacy.ts) — consent, faces policy, license plates
```

### Phase Completion

| Phase | Description | Status |
|-------|-------------|--------|
| A | Core domain: VideoProject, VideoPolicy, VideoRetention, VideoOutput, VideoTemplate | Complete |
| B | Source media model: VideoSource wrapping MediaAsset (12 source types) | Complete |
| C | Timeline model: VideoScene (10 types), VideoTimeline, ordering + duration support | Complete |
| D | Publishing profiles: VIDEO_PUBLISHING_PROFILE_REGISTRY (9 profiles) | Complete |
| E | AI compatibility: VIDEO_AI_PROVIDER_REGISTRY (8 providers), AI gate functions | Complete |
| F | Privacy model: VideoPrivacyConfig, faces/plate policies, download+deletion records | Complete |
| G | Future workflow documented in VIDEO_PIPELINE_SPEC.md | Complete |
| H | Documentation: VIDEO_PIPELINE_SPEC.md, spec index updated to v2.9 | Complete |

### Files Created

| Module | File | Purpose |
|--------|------|---------|
| Types | `video-types.ts` | All pure type definitions, default constants, PRESET_VIDEO_TEMPLATES (3) |
| Source | `video-source.ts` | VideoSource wrapping MediaAsset, permission gates, source helpers |
| Timeline | `video-timeline.ts` | VideoScene, VideoTimeline, validateTimeline(), timeline builder helpers |
| Project | `video-project.ts` | VideoProject full domain object, summary projection, eligibility checks |
| Publishing | `video-publishing.ts` | VIDEO_PUBLISHING_PROFILE_REGISTRY — 9 profiles with full specs |
| AI | `video-ai.ts` | VIDEO_AI_PROVIDER_REGISTRY (8 providers), toVideoProjectForAI() gate |
| Privacy | `video-privacy.ts` | VideoPrivacyConfig, download/deletion records, privacy helpers |
| Public API | `index.ts` | Re-exports from all video modules |

### Key Design Decisions

- `VideoSource.media_asset: MediaAsset` — pipeline always consumes canonical media domain objects; raw file paths are never used
- All 9 publishing profiles have `available_now = false` — publishing infrastructure requires Phase 11C+ work
- All 8 AI providers have `available_now = false` — integration requires CTO approval and API credentials
- `DEFAULT_VIDEO_POLICY.requires_dealer_approval = true` — dealers always approve generated video before publishing
- `DEFAULT_VIDEO_POLICY.ai_content_allowed = false` — must be explicitly enabled per project (opt-in)
- `DEFAULT_VIDEO_PRIVACY_CONFIG.ai_training_excluded = true` — AI training opt-out by default
- `DEFAULT_VIDEO_PRIVACY_CONFIG.license_plate_policy = "blur_all"` — all plates blurred by default
- No circular imports: `video-types → (none)`, `video-source → video-types + @/lib/media`, `video-timeline → video-source`, `video-project → video-source + video-timeline`, `video-ai → video-project + @/lib/ai/agents/types`

### Future AI Video Workflow (Phase G — not yet executed)

```
MediaAsset (existing, marketing_approved)
  ↓
VideoProject.sources — VideoSource[] wrapping selected MediaAssets
  ↓
[AI Gateway] toVideoProjectForAI() — consent + permission gate
  ↓
VideoProjectForStoryboard — privacy-safe projection (no raw file URLs)
  ↓
[AI Provider: Anthropic Claude] storyboard_generation — scene descriptions
  ↓
VideoTimeline — sequenced scenes with durations and transitions
  ↓ [Dealer Review of Storyboard]
[AI Provider: Runway / Kling / Pika] video_generation — clip assembly
  ↓
VideoOutput — generated video artifact (file_path set)
  ↓ [Dealer Approval]
VIDEO_PUBLISHING_PROFILE_REGISTRY — export to 1–9 publishing destinations
  ↓
VideoAnalytics — performance data from published channels
  ↓
VideoRetention / VideoDeletionRecord — scheduled deletion after retention period
```

All stages documented. No stage is executed at runtime in Sprint 11B.

### Next Steps (Sprint 11C+)

1. Implement AI Gateway routing for `storyboard_generation` capability (Anthropic Claude)
2. Implement Runway Gen-3 Alpha integration via AI Gateway for `video_generation`
3. Implement dealer approval UI for generated video
4. Implement publishing adapters for `instagram_reels` + `google_business_profile`
5. Flip `available_now = true` for first publishing profile once adapter is complete
6. Implement `VideoDownloadRecord` persistence (server action + DB table)
7. Submit `video_projects` + `video_sources` DB schema for CTO approval

---

*GYEON Detailer Agent | AI Marketing Agent Roadmap | Office AZ | 2026-06-26*
