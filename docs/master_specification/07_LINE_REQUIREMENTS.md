# 07 — LINE REQUIREMENTS

| Field | Value |
|-------|-------|
| **Version** | 1.1 |
| **Status** | Canonical |
| **Last Updated** | 2026-06-26 |
| **Canonical Source** | `gyeon_settings_flow.json` (line, line-api, auto-message, reminder-templates), `gyeon_flow.json` (STEP5 LINE転送) |
| **Related Documents** | `02_SYSTEM_ARCHITECTURE.md`, `04_SETTINGS_WORKFLOW.md`, `05_DATABASE_REQUIREMENTS.md`, `11_CANONICAL_RULES.md`, `OPERATOR_DECISIONS.md` (OD-11, OD-13) |

> Canonical inputs: `gyeon_settings_flow.json` (line, line-api, auto-message, reminder-templates) and `gyeon_flow.json` (STEP5 「LINE転送」). Implementation: `src/app/api/line/*`, `src/lib/line/*`.

---

## 1. Canonical Intent (from JSON)

### 1.1 Estimate forwarding (from `gyeon_flow.json`)
STEP5 action **「LINE転送」 → `shareToLine()`** — send the estimate as text via LINE.

### 1.2 Greeting/notice text (from settings `line`, group g5, 準備中)
- `line-header`, `line-footer` — header/footer for estimate-forward messages.
- `maint-header`, `maint-footer` — header/footer for maintenance notices.
Stored in `dealer_settings.line_message_header/footer` and `maintenance_message_header/footer`.

### 1.3 Official-account integration (settings `line-api`, 準備中)
- Channel ID, Channel Secret, Webhook URL (auto-generated by app).
- **Saved to server (D1/Supabase) only — never KV/client.**

### 1.4 Auto-message (settings `auto-message`, 準備中) — server-side only.

### 1.5 Reminder (settings `reminder-templates`)
3 templates (1/6/12-month), each with a LINE `message` body; used to send maintenance reminders. See `04_SETTINGS_WORKFLOW.md` §5.8.

> Canonical status of all LINE settings is **準備中 (hidden)** — specced but intended for later activation.

---

## 2. Implemented Components

| Concern | File / Artifact | Required Env Vars |
|---------|-----------------|-------------------|
| **Webhook** | `src/app/api/line/webhook/route.ts` | `LINE_CHANNEL_SECRET`, `LINE_CHANNEL_ID` |
| **LIFF link** | `src/app/api/line/liff/link/route.ts`, `src/app/liff/link/` | `NEXT_PUBLIC_LIFF_ID` |
| **LIFF reservation** | `src/app/liff/reservation/` | `NEXT_PUBLIC_LINE_LIFF_ID` |
| **Messaging API (push)** | `src/lib/line/send-line-message.ts`, `process-line-notification-queue.ts` | `LINE_CHANNEL_ACCESS_TOKEN` |
| **Customer linking** | `src/lib/line/link-line-customer.ts`, `get-line-customers.ts`; table `line_customers` (043) | — |
| **Message logs / queue** | `create-line-message-log.ts`, `get-line-message-logs.ts`, `queue-line-notification.ts`, `get-line-notification-queue.ts`; table `line_message_logs` (044) | — |
| **Settings** | `src/lib/line/get-line-settings.ts`, `update-line-settings.ts` | — |
| **Reminder send** | queue + `maintenance_reminders` (045) | `CRON_SECRET`, `LINE_CHANNEL_ACCESS_TOKEN` |

---

## 3. Requirements & Status

| Item | Requirement | Status |
|------|-------------|--------|
| **LIFF customer link** | Customer-facing self-link page via LIFF (`/liff/link`) | 🟡 Code ✅ — needs `LIFF_ID` |
| **LIFF reservation** | Customer self-booking page via LIFF (`/liff/reservation`) | 🟡 Code ✅ — needs `LIFF_ID` |
| **Messaging API (push)** | Programmatic push messages (staff-initiated, queued) | 🟡 Code ✅ — needs `LINE_CHANNEL_ACCESS_TOKEN` |
| **Webhook** | Inbound LINE event handler; customer auto-link by LINE ID | 🟡 Code ✅ — needs `LINE_CHANNEL_SECRET` / `CHANNEL_ID` |
| **Signature validation** | All webhook requests verified with `LINE_CHANNEL_SECRET` | ✅ Implemented |
| **Reminder** | Scheduled maintenance reminders via LINE using templates | 🟡 Code ✅ — needs `CRON_SECRET` + access token |
| **Estimate forwarding** | Share estimate text via LINE (STEP5) | 🟡 Code ✅ — needs activation |

All four LINE integration areas are **code-complete but unconfigured** locally (no LINE credentials in `.env.local`).

---

## 4. Security Rules (non-negotiable)

- `LINE_CHANNEL_SECRET` and `LINE_CHANNEL_ACCESS_TOKEN` are **server-side only** — never expose in UI, never return to client.
- Only safe public metadata (LIFF ID, Webhook URL, QR URL, connection status) may be displayed client-side.
- `getDealerSettingsPublic()` must strip `line_channel_secret` and `line_access_token` before returning.
- This matches the canonical "line-api saved to server only" rule in `gyeon_settings_flow.json`.

---

## 5. dealer_settings LINE Columns

> ⚠️ Columns marked ➕ ADD are included in migration 070 (`ADD COLUMN IF NOT EXISTS`). Apply status depends on OD-1 (see `OPERATOR_DECISIONS.md`).

| Column | Status | Security | Notes |
|--------|--------|----------|-------|
| `line_channel_id` | ✅ EXISTS | Server only | Channel ID |
| `line_channel_secret` | ✅ EXISTS | **SERVER ONLY — never expose to client** | Webhook signature validation |
| `line_access_token` | ✅ EXISTS | **SERVER ONLY — never expose to client** | Push message auth |
| `line_liff_id` | ✅ EXISTS | Public | LIFF App ID |
| `webhook_url` | ✅ EXISTS | Public | Auto-generated by app |
| `line_enabled` | ✅ EXISTS | Public | Default: false |
| `friend_add_qr_url` | ➕ ADD (mig 070) | Public | LINE友だち追加QRコードURL |
| `line_message_header` | ➕ ADD (mig 070) | Public | 見積LINE転送・冒頭文 (⚠️ see OD-11) |
| `line_message_footer` | ➕ ADD (mig 070) | Public | 見積LINE転送・末尾文 (⚠️ see OD-11) |
| `maintenance_message_header` | ➕ ADD (mig 070) | Public | メンテナンス通知・冒頭文 (⚠️ see OD-11) |
| `maintenance_message_footer` | ➕ ADD (mig 070) | Public | メンテナンス通知・末尾文 (⚠️ see OD-11) |
| `sns_urls` | ➕ ADD (mig 070) | Public | `{instagram, x, google, line}` JSONB |
| `line_public_settings` | ➕ ADD (mig 070) | **MUST NOT contain secrets** | Public-safe LINE metadata; `getDealerSettingsPublic()` may include this |
| `line_message_templates` | ➕ ADD (mig 070) | Public | Structured templates: `{estimate_sent:{header,footer}, maintenance_reminder:{header,footer}}` (⚠️ see OD-11) |

> ⚠️ **OD-11 — Dual-path ambiguity:** Both individual text columns (`line_message_header/footer`) and the structured JSONB column (`line_message_templates`) serve the same purpose. The application must write to exactly one. See `OPERATOR_DECISIONS.md` OD-11 before implementing LINE message UI.

---

## 6. LINE Rich Menu (Pro+ Feature)

**Status:** Implemented (foundation). **Plan tier:** Pro+ only.

### 6.1 Architecture
- Config stored in `dealer_settings.line_public_settings.rich_menu` (PHASE70 JSONB column).
- **No new migration required** — uses existing `line_public_settings` column from migration 070.
- Pro+ feature gate: `line_rich_menu` in `AppFeature`, gated by `checkFeatureAccess()`.

### 6.2 Files
| File | Purpose |
|------|---------|
| `src/lib/line/line-rich-menu-types.ts` | Types + `parseRichMenuConfig()` |
| `src/lib/line/generate-rich-menu-png.ts` | Server-side PNG generator (pure Node.js zlib) |
| `src/lib/line/get-line-rich-menu-config.ts` | Server action: read config from DB |
| `src/lib/line/save-line-rich-menu-config.ts` | Server action: save config (preserves richMenuId) |
| `src/lib/line/publish-line-rich-menu.ts` | Server action: create menu → upload PNG → set default |
| `src/lib/line/delete-line-rich-menu.ts` | Server action: remove from LINE + clear DB |
| `src/components/settings/LineRichMenuSettings.tsx` | Settings UI component |

### 6.3 Template Image (v1)
- 2500×1686px PNG, 6 colored zones in a 3×2 grid (GYEON brand palette).
- Generated server-side using Node.js `zlib` — no external image library required.
- v1 limitation: button labels are not rendered in the image.
- Custom image upload: planned for a future version.

### 6.4 6-Button Default Template
| Position | Default Label | Default Action |
|----------|--------------|----------------|
| Top-left | 予約する | URI (configurable) |
| Top-center | 施工メニュー | URI (configurable) |
| Top-right | メンテナンス | URI (configurable) |
| Bottom-left | レビュー投稿 | URI (configurable) |
| Bottom-center | 施工事例 | URI (configurable) |
| Bottom-right | お問い合わせ | Message: "お問い合わせ" |

### 6.5 Security rules
- `line_access_token` is read server-side only in all server actions — never returned to client.
- `dealer_id` sourced from `getCurrentDealer()` — never from client form input.
- Pro+ access verified server-side via `checkFeatureAccess("line_rich_menu")`.

---

## 7. Operator Decisions Required

1. **Activate group g5** (currently 準備中/hidden) — decide when LINE settings become visible and editable in-app.
2. **Provision LINE credentials** (Channel ID, Channel Secret, Access Token, LIFF ID) to activate all §3 items.
3. **Confirm reminder scheduling mechanism** — cron/edge function + `CRON_SECRET`.
4. **Execute dealer_settings Migration 002** (LINE & SNS column additions) — requires CTO approval and staging verification first.
